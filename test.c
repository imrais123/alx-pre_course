#include <stdio.h>
main (void);
{
int age;
ptintf("%d\n", "enter your age here:");
scanf("%d", &age);
}
if (age > 50)
{
printf("your age is not supportted"\n);
}
else {
printf("your age is:", age);
return (0);
}
